import setuptools

setuptools.setup(
    name="projectName", # Replace with your own project
    version="0.0.1",
    author="Mitchell Dawson",
    description="Example python project folder",
    packages=setuptools.find_packages(),
    python_requires='>=3.6',
)
